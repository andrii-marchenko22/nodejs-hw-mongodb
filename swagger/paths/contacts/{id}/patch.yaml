tags:
  - Contacts
summary: Update a contact
operationId: updateContact
description: 'Update one or more fields of a contact by its unique identifier'
security:
  - bearerAuth: []
parameters:
  - in: path
    name: contactId
    required: true
    description: The unique ID of the contact to update
    schema:
      type: string
      example: '65ca67e7ae7f10c88b598384'
requestBody:
  required: true
  content:
    application/json:
      schema:
        type: object
        properties:
          name:
            description: "Contact's name (3-20 characters)"
            type: string
            minLength: 3
            maxLength: 20
            example: 'John Doe'
          phoneNumber:
            description: "Contact's phone number (+380000000000 format, 6-15 digits)"
            type: string
            pattern: '^\\+?\\d{6,15}$'
            example: '+380000000000'
          email:
            description: "Contact's email"
            type: string
            format: email
            example: 'test@example.com'
          contactType:
            description: 'Type of contact'
            type: string
            enum:
              - work
              - home
              - personal
            example: 'work'
responses:
  '200':
    description: Successfully updated the contact
    content:
      application/json:
        schema:
          type: object
          required:
            - status
            - message
            - data
          properties:
            status:
              type: integer
              example: 200
            message:
              type: string
              example: Successfully updated contact!
            data:
              type: object
              $ref: '../../../components/schemas/contact.yaml'
  '401':
    $ref: '../../../components/responses/401.yaml'
  '404':
    description: Contact not found
  '400':
    description: Bad request. Validation failed or contactId is not a valid MongoDB ObjectId.
