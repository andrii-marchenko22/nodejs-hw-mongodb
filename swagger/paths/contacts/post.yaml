tags:
  - Contacts
summary: Create contact
operationId: createContact
description: 'Create a contact with payload matching Joi validation schema'
security:
  - bearerAuth: []
requestBody:
  required: true
  content:
    multipart/form-data:
      schema:
        type: object
        required:
          - name
          - phoneNumber
          - email
          - contactType
        properties:
          name:
            description: "Contact's name (3-20 characters)"
            type: string
            minLength: 3
            maxLength: 20
            example: 'John Doe'
          phoneNumber:
            description: "Contact's phone number (+380000000000 format, 6-15 digits)"
            type: string
            pattern: '^\\+?\\d{6,15}$'
            example: '+380000000000'
          email:
            description: "Contact's email"
            type: string
            format: email
            example: 'test@example.com'
          contactType:
            description: 'Type of contact'
            type: string
            enum:
              - work
              - home
              - personal
            example: 'work'
          isFavourite:
            description: 'Whether the contact is marked as favourite (optional)'
            type: boolean
            example: false
          userId:
            description: 'ID of the user who creates the contact (optional, must be valid MongoDB ObjectId)'
            type: string
            example: '65e4decdd286b30065d54af9'
          photo:
            description: "Contact's photo (optional)"
            type: string
            format: binary
responses:
  '201':
    description: Successfully created a contact
    content:
      application/json:
        schema:
          type: object
          required:
            - status
            - message
            - data
          properties:
            status:
              type: integer
              example: 201
            message:
              type: string
              example: Successfully created a contact!
            data:
              type: object
              $ref: '../../components/schemas/contact.yaml'
  '401':
    $ref: '../../components/responses/401.yaml'
  '400':
    description: Bad request, validation failed
